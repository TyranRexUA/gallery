export interface imageType {
    image_id: number
    src: string
}

export interface imageSrcType {
    src: string
}
export interface commentType {

    description: string
    name: string
    id: number
    image_id: number

}